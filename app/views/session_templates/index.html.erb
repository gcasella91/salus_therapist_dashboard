<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All sessions
    </h1>

    <a href="/sessions/new" class="btn btn-block btn-outline-secondary">
      Add a new session
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "session_templates/sessions_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Therapist
        </th>

        <th>
          Patient
        </th>

        <th>
          Date
        </th>

        <th>
          Notes
        </th>

        <th>
          Price
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @sessions.each do |session| %>
      <tr>
        <!-- Display session.id start -->
        <td>
          <%= session.id %>
        </td>
        <!-- Display session.id end -->

        <!-- Display session.therapist_id start -->
        <td>
          <% if session.therapist.present? %>
            <a href="/users/<%= session.therapist.id %>">
              <%= session.therapist.email %>
            </a>
          <% end %>
        </td>
        <!-- Display session.therapist_id end -->

        <!-- Display session.patient_id start -->
        <td>
          <% if session.patient.present? %>
            <a href="/patients/<%= session.patient.id %>">
              <%= session.patient.patient_name %>
            </a>
          <% end %>
        </td>
        <!-- Display session.patient_id end -->

        <!-- Display session.date start -->
        <td>
          <%= session.date %>
        </td>
        <!-- Display session.date end -->

        <!-- Display session.notes start -->
        <td>
          <%= session.notes %>
        </td>
        <!-- Display session.notes end -->

        <!-- Display session.price start -->
        <td>
          <%= session.price %>
        </td>
        <!-- Display session.price end -->

        <!-- Display session.created_at start -->
        <td>
          <%= time_ago_in_words(session.created_at) %> ago
        </td>
        <!-- Display session.created_at end -->

        <!-- Display session.updated_at start -->
        <td>
          <%= time_ago_in_words(session.updated_at) %> ago
        </td>
        <!-- Display session.updated_at end -->

        <td>
          <a href="/sessions/<%= session.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @sessions, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
